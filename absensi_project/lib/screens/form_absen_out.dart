import 'package:absensi_project/form_absen_in_component/body.dart';
import 'package:absensi_project/form_absen_out_component/body.dart';
import 'package:flutter/material.dart';

class FormAbsenOut extends StatelessWidget {
  const FormAbsenOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyFormOut(),
    );
  }
}
