import 'package:absensi_project/form_absen_in_component/body.dart';
import 'package:flutter/material.dart';

class FormAbsenIN extends StatelessWidget {
  const FormAbsenIN({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyFormIn(),
    );
  }
}
